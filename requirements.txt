# AURA - Production Dependencies (Pinecone + Supabase + Gemini)

# Core Framework
streamlit==1.32.0

# Data Processing
pandas==2.1.0
numpy==1.26.0

# Visualization
plotly==5.18.0

# AI/ML - Embeddings & Reranking (FREE, runs locally)
sentence-transformers==2.5.1
torch==2.1.0
transformers==4.36.0

# Vector Database - Pinecone (FREE 100k vectors)
pinecone-client==3.0.0
langchain-pinecone==0.0.3

# Vector Database - ChromaDB (fallback if Pinecone unavailable)
chromadb==0.4.22
langchain-chroma==0.1.0

# LangChain core
langchain-core==0.1.40
langchain-huggingface==0.0.3
langchain-text-splitters==0.0.1

# Structured Database - Supabase (FREE 500MB)
supabase==2.0.3

# LLM - Google Gemini (FREE 1500 req/day)
google-generativeai==0.4.0

# Utilities
python-dotenv==1.0.0
